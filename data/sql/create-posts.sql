CREATE TABLE posts (
	pid INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
	author INTEGER NOT NULL,
	neighbourhood INTEGER NOT NULL,
	posted_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
	content VARCHAR(1000),
	attachment VARCHAR(125)
);